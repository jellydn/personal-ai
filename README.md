# personal-ai

> Temporary branch note: a mistaken placeholder commit landed here. Prefer the Copilot/catalog PR from `main` for the real update (token-pulse, signal-gate, learning-hub). Do not merge this branch as-is.
